# Claude Code 项目规则

## 自动加载的规则文件
请在每次对话开始时自动读取并严格遵守以下规则文件：

- `.kiro/steering/rule.md` - 项目代码规范（最高优先级）

## 规则说明
1. rule.md 包含项目的所有编码规范、架构原则和协作流程
2. 当涉及特定领域（如资源加载、事件系统、NPC任务等）时，需按 rule.md 第 15 条的流程询问用户是否查看对应 skill
3. 所有代码修改必须符合 rule.md 中的约定

## Skill 文件位置
- `.claude/skills/` 目录下的各个 SKILL.md 文件
- 按需读取，不自动加载
